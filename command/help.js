function helpHelper(){
    console.log(`List of all the commands:
                1. view
                     a:) tree
                     b:) flat
                2. organize [folder_path/_]
                3. help 
                `);
}

module.exports = {
   // fn : helpHelper
   helpHelper
};